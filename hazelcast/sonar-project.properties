# Required metadata
sonar.projectKey=galeb-hazelcast
sonar.projectName=galeb-hazelcast
sonar.projectVersion=3.1.10-SNAPSHOT

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8
